<%= form_with(model: @type, local: true) do |form| %>
 
  <% if @type.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@type.errors.count, "error") %> prohibited
        this Type from being saved:
      </h2>
      <ul>
        <% @type.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :description %>
  </p>
  
  <p>
    <%= form.submit %>
  </p>
 
<% end %>